package group9.trump.model;

import group9.trump.model.Shitinarabe;
import group9.trump.model.ShitinarabeMapper;
import group9.trump.model.Smatch;
import group9.trump.model.SmatchMapper;

public class Sgame {
  public void Sgame(Shitinarabe card) {
    Shitinarabe right = new Shitinarabe();
    Shitinarabe left = new Shitinarabe();

    /*
     * if(card.getNumber() == 13){
     * right = selectById(card.getNumber() - 12, card.getMark());
     * }else{
     * right = selectAllById(card.getNumber() + 1 , card.getMark());
     * }
     * if(card.getNumber() == 1){
     * left = selectAllById(card.getNumber() + 12, card.getMark());
     * }else{
     * right = selectAllById(card.getNumber() - 1 , card.getMark());
     * }
     * 
     * if(right.getState == "tehuda" || left.getState == "tehuda"){
     * return false;
     * }else{
     * f
     * return true;
     * }
     */
  }
}
